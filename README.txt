You won't need to edit dashboard.html or depatures.json

To edit and run the code use just trip_planner_departure_monitoring.py

Before the code will work you'll have to update the filepaths in the python file to match where you've put them on your computer

Before the code will work you'll have to put your API key in the python file inside the double quotes

In the code you'll see the def main(): function. Replace the stop_id with the stop ID of the station you care about. For now I've put all the stations for Central except Ferries which has Circular Quay.

I haven't yet added in the metro, but it should be simple enough using ChatGPT.

To view the dashboard open dashboard.html using any web browser

If you run into any trouble just honestly use ChatGPT - it's very handy